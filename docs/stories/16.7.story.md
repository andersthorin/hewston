# Story 16.7 — Idempotency via plan_hash

Goal
- Ensure start_agentic_run is idempotent using plan_hash (ADR-008).

Acceptance Criteria
- Duplicate submissions with same plan_hash return existing run_ids/manifests
- plan_hash computed over normalized Plan JSON (versioned)

Tasks
- Hashing utility + normalization rules
- Duplicate detection + response behavior
- Tests

Dependencies
- Stories 16.2, 16.4; ADR-008

