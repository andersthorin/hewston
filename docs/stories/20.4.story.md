# Story 20.4 — Streaming Protocol Smoke Test

Goal
- Validate streaming run protocol on longer ranges.

Acceptance Criteria
- Sequence: add initial data, run(streaming=True), clear_data(), add next batch, final run(streaming=False) or end()
- No memory exhaustion on M1 for configured chunks

Tasks
- Implement test driver and assertions
- Measure memory footprint

Dependencies
- 20.2

